spring:
  application:
    name: spring-cloud-eureka
    
server:
  port: 8000
  
eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
    service-url:
      defaultZone: http://localhost:peer2:8001/eureka/
  instance:
    hostname: peer1 